import { BasicList, ListItem, ListTask } from 'coc.nvim';
export declare class WebviewList extends BasicList {
    readonly name = "webview";
    readonly defaultAction = "open";
    description: string;
    constructor();
    loadItems(): Promise<ListItem[] | ListTask | null | undefined>;
}
